# 2026-06-25 知识库优化

## 做了什么

### 1. 统一 Vault 与知识库 docs
- 用 Windows Junction（`mklink /J`）将 `knowledge-base/docs/` 联结到 `D:\Agent\Obsidian store`
- 两个路径指向同一份文件，写 vault 即写 docs，无需复制
- `config.py` 的 `DOC_DIR` 保持 `os.path.join(BASE_DIR, "docs")`

### 2. 增量入库机制
- 已有 `incremental_ingest.py`：MD5 对比 manifest，只处理新增/修改/删除
- 每次往 vault 写文件后自动跑增量入库

### 3. 投资策略总览
- 从知识库检索并整理了《韭菜修行记》+ 选股交易系统项目日志中的策略要点
- 输出 `投资策略总览.md`（七大板块：技术指标、交易战法、选股条件、止盈止损、市场环境、心态习惯、技术实现）

### 4. CLAUDE.md 更新
- 记录了 Junction 说明
- 入库铁律：只跑增量不跑全量
- 每次写 vault 后自动入库
- 保存对话日志到 vault 的命令
